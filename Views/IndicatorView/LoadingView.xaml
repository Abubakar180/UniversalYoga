<?xml version="1.0" encoding="utf-8" ?>
<Mopups:PopupPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 x:Class="UniversalYoga.Views.IndicatorView.LoadingView"
                 xmlns:Animations="clr-namespace:Mopups.Animations;assembly=Mopups"
                 xmlns:Mopups="clr-namespace:Mopups.Pages;assembly=Mopups"
                 BackgroundColor="#77000000"
                 CloseWhenBackgroundIsClicked="False">
    <!--This is a Custom Popup Page-->
    <Mopups:PopupPage.Animation>
        <Animations:ScaleAnimation
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"
            PositionIn="Center"
            PositionOut="Center"
            ScaleIn="1.2"
            ScaleOut="0.8" />
    </Mopups:PopupPage.Animation>
    <StackLayout VerticalOptions="Center" HorizontalOptions="Center">

        <ActivityIndicator x:Name="actind"  Color="{StaticResource SecondoryColor}" IsRunning="True" IsVisible="true" 
                           HeightRequest="30"
                           WidthRequest="30"/>

        <Label Text="Please Wait" TextColor="White" FontFamily="Texta" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" FontSize="18"></Label>

    </StackLayout>
</Mopups:PopupPage>